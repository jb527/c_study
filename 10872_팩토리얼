#include <stdio.h>

int main(){
    int N, sum;
    scanf("%d", &N);
    sum = 1;
    if (N == 0)
        printf("1");
    else{
        for(int i = 0; i < N; i++){
            sum *= (i+1);
        }
        printf("%d", sum);}
    return 0;
}
